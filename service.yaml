---
apiVersion: v1
kind: Service
metadata:
  name: staragile-service
  labels:
    app: staragile
spec:
  type: NodePort
  selector:
    app: staragile
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
  
